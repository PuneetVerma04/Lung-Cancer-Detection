# Minimal requirements inferred from notebooks in this repo
# Adjust versions for your environment (GPU vs CPU builds for torch/tensorflow).

numpy>=1.21
pandas>=1.3
matplotlib>=3.4
scikit-learn>=1.0
scipy>=1.7
opencv-python>=4.5
Pillow>=9.0
scikit-image>=0.19

# TensorFlow (includes Keras). Use appropriate build for your GPU/CPU.
tensorflow>=2.9

# PyTorch and torchvision (pick compatible versions for your CUDA/toolkit)
torch>=1.13
torchvision>=0.14

# Jupyter / notebook tooling
jupyter>=1.0
ipykernel
notebook

# Convenience
tqdm>=4.60
seaborn>=0.11

# Optional: install git-lfs if you plan to track model binaries with LFS
# git-lfs (system-level, not a pip package)
